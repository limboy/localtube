{
  "name": "localtube",
  "private": true,
  "version": "0.7.0",
  "description": "LocalTube",
  "main": "electron/dist/main.js",
  "type": "commonjs",
  "workspaces": [
    "src-web"
  ],
  "scripts": {
    "dev": "concurrently -k -n vite,electron -c blue,green \"npm run dev -w src-web\" \"tsc -p electron && wait-on http://localhost:1422 && electron ./electron/dist/main.js\"",
    "build": "npm run build -w src-web && tsc -p electron && env-cmd -f .env electron-builder --mac --arm64 --x64"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "concurrently": "^9.1.0",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "env-cmd": "^10.1.0",
    "tsx": "^4.19.2",
    "typescript": "~5.6.2",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "electron-updater": "^6.3.9",
    "electron-window-state": "^5.0.3"
  }
}